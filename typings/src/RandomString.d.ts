/**
 * RandomString
 * Generates Random String
 * @example
 * ```js
 * const {RandomString} = require("sonicware.js");
 * console.log(RandomString(25));
 * ```
 * @function
 */
export declare const RandomString: (Length: number | null) => string;
